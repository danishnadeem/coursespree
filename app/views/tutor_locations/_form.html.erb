<%= form_for(@tutor_location) do |f| %>
  <% if @tutor_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tutor_location.errors.count, "error") %> prohibited this tutor_location from being saved:</h2>

      <ul>
      <% @tutor_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :university_id %><br />
    <%= f.select :university_id, univs_and_ids %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
