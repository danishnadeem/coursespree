<%= form_for(@subjects_tutor) do |f| %>
  <% if @subjects_tutor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subjects_tutor.errors.count, "error") %> prohibited this subjects_tutor from being saved:</h2>

      <ul>
      <% @subjects_tutor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %><br />
    <%= f.number_field :subject_id %>
  </div>
  <div class="field">
    <%= f.label :tutor_id %><br />
    <%= f.number_field :tutor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
